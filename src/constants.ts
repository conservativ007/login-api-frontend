const URL = 'http://localhost:4000/api'

export const CONSTANTS = {
	urlAuth: `${URL}/auth`,
	signup: `${URL}/auth/signup`,
	login: `${URL}/auth/login`,
	logout: `${URL}/auth/logout`,
	profile: `${URL}/auth/profile`,
	refresh: `${URL}/auth/refresh`,
	reload: `${URL}/jwt/decode`
}
